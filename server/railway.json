{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "uvicorn app.main:app --app-dir server --reload",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
